import {useState} from 'react'
import {X} from 'lucide-react'

type Props={open:boolean;onClose:()=>void;onCreate:(input:{customer:string;phone:string;initialMessage?:string})=>void}
export default function NewSupportConversationModal({open,onClose,onCreate}:Props){const [customer,setCustomer]=useState(''),[phone,setPhone]=useState(''),[message,setMessage]=useState('');if(!open)return null;const submit=()=>{if(!phone.trim())return;onCreate({customer,phone,initialMessage:message});setCustomer('');setPhone('');setMessage('')};return <div className="chat-modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className="chat-modal chat-modal-sm" role="dialog" aria-modal="true" aria-label="Nova conversa via WhatsApp"><header><h2>Nova conversa via WhatsApp</h2><button onClick={onClose} aria-label="Fechar"><X size={17}/></button></header><div className="chat-modal-body"><p className="chat-help">Neste ambiente de desenvolvimento, as mensagens são registradas no Portal Lander para simulação operacional. Nenhum provedor externo de WhatsApp está conectado ainda, portanto não há entrega real ao cliente.</p><label className="chat-field"><span>Nome do contato</span><input value={customer} onChange={e=>setCustomer(e.target.value)} placeholder="Opcional"/></label><label className="chat-field"><span>Telefone (WhatsApp)</span><input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="+55 11 99999-9999"/></label><label className="chat-field"><span>Mensagem inicial</span><textarea rows={3} value={message} onChange={e=>setMessage(e.target.value)} placeholder="Opcional — a conversa pode ser criada sem registrar uma mensagem inicial"/></label></div><footer><button className="button outline" onClick={onClose}>Cancelar</button><button className="button dark" disabled={!phone.trim()} onClick={submit}>Iniciar conversa</button></footer></section></div>}
