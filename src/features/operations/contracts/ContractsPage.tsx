import { AlertCircle, CheckCircle, Clock, DollarSign, FileStack, FileText, MoreHorizontal, PenLine, Plus, Search, Send, X } from 'lucide-react'
import { useMemo, useState, type ReactNode } from 'react'
import { CRM_NAV } from '../../../shared/internal/adminNavigation'
import { AdminShell } from '../../../shared/internal/AdminUi'

type Contract={id:string;title:string;client:string;type:string;platform:string;status:string;start:string;end:string;value:number}
type WizardState={template:string;category:string;contractor:string;contracted:string;variables:Record<string,string>;content:string;signers:{name:string;email:string;order:number;platform:string}[];title:string;start:string;end:string;status:string;notes:string}
const INITIAL:Contract[]=[
  {id:'ctr-1',title:'Pacote de mídia · Norte Produções',client:'Norte Produções',type:'Publicidade',platform:'Autentique',status:'Aguardando Assinatura',start:'2026-09-01',end:'2026-09-30',value:18000},
  {id:'ctr-2',title:'Cobertura · Festival Órbita',client:'Festival Órbita',type:'Evento',platform:'—',status:'Rascunho',start:'2026-09-10',end:'2026-09-12',value:12000},
  {id:'ctr-3',title:'Parceria · Studio Sul',client:'Studio Sul',type:'Parceria',platform:'Autentique',status:'Vigente',start:'2026-08-01',end:'2027-07-31',value:9000},
]
const EMPTY_WIZARD:WizardState={template:'Contrato de Prestação de Serviços',category:'Comercial',contractor:'Portal Lander',contracted:'',variables:{'{{EMPRESA.RAZAO_SOCIAL}}':'Portal Lander','{{CLIENTE.NOME}}':'','{{CONTRATO.NUMERO}}':'CTR-2026-004','{{CONTRATO.VALOR}}':''},content:'',signers:[{name:'',email:'',order:1,platform:'autentique'}],title:'',start:'',end:'',status:'Rascunho',notes:''}
const money=(value:number)=>value.toLocaleString('pt-BR',{style:'currency',currency:'BRL'})
function Section({title,children}:{title:string;children:ReactNode}){return <section className="reference-form-section"><h3>{title}</h3>{children}</section>}

export function ContractsPage(){
  const [contracts,setContracts]=useState<Contract[]>(INITIAL)
  const [query,setQuery]=useState('')
  const [type,setType]=useState('Todos os tipos')
  const [status,setStatus]=useState('Todos os status')
  const [platform,setPlatform]=useState('Todas as plataformas')
  const [selected,setSelected]=useState<string[]>([])
  const [wizard,setWizard]=useState(false)
  const [step,setStep]=useState(1)
  const [wizardState,setWizardState]=useState<WizardState>(EMPTY_WIZARD)
  const [viewing,setViewing]=useState<Contract|null>(null)
  const [viewTab,setViewTab]=useState('resumo')
  const [signing,setSigning]=useState(false)
  const [signMessage,setSignMessage]=useState('Olá, segue o contrato para assinatura digital.')
  const [signDeadline,setSignDeadline]=useState('')
  const filtered=useMemo(()=>{const q=query.trim().toLocaleLowerCase('pt-BR');return contracts.filter(c=>(!q||`${c.title} ${c.client}`.toLocaleLowerCase('pt-BR').includes(q))&&(type==='Todos os tipos'||c.type===type)&&(status==='Todos os status'||c.status===status)&&(platform==='Todas as plataformas'||c.platform===platform))},[contracts,query,type,status,platform])
  const counts={vigentes:contracts.filter(c=>c.status==='Vigente').length,assinados:contracts.filter(c=>c.status==='Assinado').length,aguardando:contracts.filter(c=>c.status==='Aguardando Assinatura').length,analise:contracts.filter(c=>!['Vigente','Assinado','Aguardando Assinatura','Encerrado'].includes(c.status)).length,encerrados:contracts.filter(c=>c.status==='Encerrado').length}
  const totalValue=contracts.filter(c=>['Vigente','Assinado'].includes(c.status)).reduce((sum,c)=>sum+c.value,0)
  const allSelected=filtered.length>0&&selected.length===filtered.length
  const toggleAll=()=>setSelected(allSelected?[]:filtered.map(c=>c.id))
  const clearFilters=()=>{setQuery('');setType('Todos os tipos');setStatus('Todos os status');setPlatform('Todas as plataformas')}
  const hasFilters=Boolean(query)||type!=='Todos os tipos'||status!=='Todos os status'||platform!=='Todas as plataformas'
  const removeSelected=()=>{setContracts(items=>items.filter(c=>!selected.includes(c.id)));setSelected([])}
  const openWizard=()=>{setWizardState(EMPTY_WIZARD);setStep(1);setWizard(true)}
  const createContract=()=>{const title=wizardState.title.trim()||`${wizardState.template} · ${wizardState.contracted||'Novo cliente'}`;setContracts(items=>[{id:`ctr-${Date.now()}`,title,client:wizardState.contracted||'—',type:wizardState.category,platform:wizardState.signers[0]?.platform==='none'?'—':wizardState.signers[0]?.platform==='autentique'?'Autentique':wizardState.signers[0]?.platform==='clicksign'?'Clicksign':'DocuSign',status:wizardState.status,start:wizardState.start||new Date().toISOString().slice(0,10),end:wizardState.end||new Date().toISOString().slice(0,10),value:Number(wizardState.variables['{{CONTRATO.VALOR}}']||0)},...items]);setWizard(false)}
  const updateSigner=(index:number,key:'name'|'email'|'platform',value:string)=>setWizardState(state=>({...state,signers:state.signers.map((s,i)=>i===index?{...s,[key]:value}:s)}))
  const sendForSigning=()=>{if(viewing){setContracts(items=>items.map(c=>c.id===viewing.id?{...c,status:'Aguardando Assinatura',platform:c.platform==='—'?'Autentique':c.platform}:c));setViewing({...viewing,status:'Aguardando Assinatura',platform:viewing.platform==='—'?'Autentique':viewing.platform})}setSigning(false)}
  return <AdminShell area="crm" items={CRM_NAV} header={{title:'Contratos',description:'Gerencie contratos e documentação legal'}}>
    <div className="zip-stack contracts-page">
      <div className="zip-kpi-grid contracts">{[
        ['Total de Contratos',String(contracts.length),'na base',<FileStack size={18}/>],['Vigentes',String(counts.vigentes),'em vigor',<CheckCircle size={18}/>],['Assinados',String(counts.assinados),'aguardando vigência',<PenLine size={18}/>],['Aguardando Assinatura',String(counts.aguardando),'pendentes de assinar',<Clock size={18}/>],['Em Análise',String(counts.analise),'rascunho / negociação',<FileText size={18}/>],['Encerrados',String(counts.encerrados),'expirados / rescindidos / cancelados',<AlertCircle size={18}/>],['Valor Total',money(totalValue),'vigentes + assinados',<DollarSign size={18}/>],
      ].map(([title,value,description,icon])=><article className="zip-metric" key={String(title)}><div className="zip-metric-icon">{icon}</div><div><span>{title}</span><strong>{value}</strong><small>{description}</small></div></article>)}</div>
      <div className="zip-toolbar contracts-toolbar"><label className="zip-search"><Search size={14}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar por artista, tipo ou título…"/></label><select value={type} onChange={e=>setType(e.target.value)}><option>Todos os tipos</option><option>Agenciamento</option><option>Distribuição</option><option>Licenciamento</option><option>Edição</option><option>Publicidade</option><option>Evento</option><option>Parceria</option></select><select value={status} onChange={e=>setStatus(e.target.value)}><option>Todos os status</option><option>Assinado</option><option>Vigente</option><option>Aguardando Assinatura</option><option>Pendente</option><option>Rascunho</option><option>Expirado</option><option>Rescindido</option><option>Cancelado</option></select><select value={platform} onChange={e=>setPlatform(e.target.value)}><option>Todas as plataformas</option><option>Autentique</option><option>Clicksign</option><option>DocuSign</option><option>—</option></select>{hasFilters&&<button className="zip-button secondary" type="button" onClick={clearFilters}><X size={14}/> Limpar</button>}<button className="zip-button" type="button" onClick={openWizard}><Plus size={14}/> Novo Contrato</button></div>
      <section className="zip-panel contracts-table-panel"><header className="zip-panel-head"><div><h2>Lista de Contratos</h2><p>Acompanhe todos os contratos e seus vencimentos · {filtered.length} registro(s)</p></div><div className="contracts-inline-actions"><label><input type="checkbox" checked={allSelected} onChange={toggleAll}/> {selected.length?`${selected.length} selecionado(s)`:'Selecionar todos'}</label>{selected.length>0&&<button className="zip-button secondary" type="button" onClick={removeSelected}>Excluir ({selected.length})</button>}</div></header><div className="zip-table-wrap"><table className="zip-table"><thead><tr><th></th><th>Título</th><th>Artista / Cliente</th><th>Tipo</th><th>Plataforma</th><th>Status</th><th>Período</th><th>Valor</th><th>Ações</th></tr></thead><tbody>{filtered.length?filtered.map(c=><tr key={c.id}><td><input type="checkbox" checked={selected.includes(c.id)} onChange={()=>setSelected(ids=>ids.includes(c.id)?ids.filter(id=>id!==c.id):[...ids,c.id])}/></td><td><strong>{c.title}</strong></td><td>{c.client}</td><td>{c.type}</td><td>{c.platform}</td><td><span className={`zip-badge ${c.status==='Vigente'?'zip-badge-success':c.status==='Aguardando Assinatura'?'zip-badge-warning':''}`}>{c.status}</span></td><td>{new Date(c.start).toLocaleDateString('pt-BR')} – {new Date(c.end).toLocaleDateString('pt-BR')}</td><td>{money(c.value)}</td><td><button className="zip-icon" type="button" title="Abrir contrato" onClick={()=>{setViewing(c);setViewTab('resumo')}}><MoreHorizontal size={15}/></button></td></tr>):<tr><td colSpan={9} className="contracts-empty-row">{hasFilters?'Nenhum contrato corresponde aos filtros aplicados.':'Nenhum contrato cadastrado.'}</td></tr>}</tbody></table></div></section>
    </div>

    {wizard&&<div className="reference-modal-backdrop"><section className="reference-modal reference-modal-xl contracts-wizard-modal"><header className="reference-modal-head"><div><span>NOVO CONTRATO</span><h2>Assistente · Etapa {step} de 6</h2></div><button className="reference-modal-close" onClick={()=>setWizard(false)}><X size={17}/></button></header><div className="contracts-wizard-layout"><aside className="contracts-wizard-steps">{['Template','Partes','Variáveis','Documento','Signatários','Revisão'].map((name,index)=><button type="button" key={name} className={step===index+1?'active':''} disabled={index+1>step} onClick={()=>index+1<=step&&setStep(index+1)}><span>{index+1}</span><div><strong>{name}</strong><small>{['Escolha a base','Defina os envolvidos','Preencha placeholders','Revise o conteúdo','Configure assinaturas','Confirme e conclua'][index]}</small></div></button>)}</aside><div className="reference-modal-body reference-form contracts-wizard-content">
      {step===1&&<Section title="Template do contrato"><div className="reference-form-grid"><label className="wide"><span>Template</span><select value={wizardState.template} onChange={e=>setWizardState(s=>({...s,template:e.target.value}))}><option>Contrato de Prestação de Serviços</option><option>Contrato de Publicidade</option><option>Contrato de Parceria Comercial</option><option>Contrato de Licenciamento</option></select></label><label><span>Categoria</span><select value={wizardState.category} onChange={e=>setWizardState(s=>({...s,category:e.target.value}))}><option>Comercial</option><option>Prestação de Serviços</option><option>Eventos</option><option>Licenciamento</option></select></label><label><span>Número</span><input value={wizardState.variables['{{CONTRATO.NUMERO}}']} onChange={e=>setWizardState(s=>({...s,variables:{...s.variables,'{{CONTRATO.NUMERO}}':e.target.value}}))}/></label></div></Section>}
      {step===2&&<Section title="Partes do contrato"><div className="reference-form-grid"><label><span>Contratante</span><input value={wizardState.contractor} onChange={e=>setWizardState(s=>({...s,contractor:e.target.value}))} placeholder="Nome / Razão social"/></label><label><span>Contratada / Cliente</span><input value={wizardState.contracted} onChange={e=>setWizardState(s=>({...s,contracted:e.target.value,variables:{...s.variables,'{{CLIENTE.NOME}}':e.target.value}}))} placeholder="Nome / Razão social"/></label><label><span>CPF / CNPJ</span><input placeholder="Documento"/></label><label><span>E-mail</span><input type="email" placeholder="email@exemplo.com"/></label><label className="wide"><span>Endereço</span><input placeholder="Rua, número, cidade - UF"/></label></div></Section>}
      {step===3&&<Section title="Variáveis do template"><div className="reference-form-grid">{Object.entries(wizardState.variables).map(([key,value])=><label key={key}><span>{key}</span><input value={value} onChange={e=>setWizardState(s=>({...s,variables:{...s.variables,[key]:e.target.value}}))} placeholder={key.includes('VALOR')?'0,00':'Preencher variável'}/></label>)}</div></Section>}
      {step===4&&<><Section title="Conteúdo do contrato"><div className="reference-form-grid"><label className="wide"><span>Documento</span><textarea rows={14} value={wizardState.content} onChange={e=>setWizardState(s=>({...s,content:e.target.value}))} placeholder="Escreva ou cole o conteúdo do contrato usando {{GRUPO.CAMPO}} para variáveis dinâmicas."/></label><label className="wide"><span>Importar documento</span><input type="file" accept=".pdf,.doc,.docx,.txt"/></label></div></Section><div className="reference-contract-preview"><div className="reference-a4"><h1>{wizardState.template.toUpperCase()}</h1><p>{wizardState.content||'Pré-visualização A4 do conteúdo contratual.'}</p></div></div></>}
      {step===5&&<Section title="Signatários">{wizardState.signers.map((signer,index)=><div className="reference-form-grid" key={index}><label><span>Nome</span><input value={signer.name} onChange={e=>updateSigner(index,'name',e.target.value)} placeholder="Nome completo"/></label><label><span>E-mail</span><input type="email" value={signer.email} onChange={e=>updateSigner(index,'email',e.target.value)} placeholder="email@exemplo.com"/></label><label><span>Ordem</span><input readOnly value={signer.order}/></label><label><span>Plataforma</span><select value={signer.platform} onChange={e=>updateSigner(index,'platform',e.target.value)}><option value="none">Sem plataforma</option><option value="autentique">Autentique</option><option value="clicksign">Clicksign</option><option value="docusign">DocuSign</option></select></label></div>)}<button type="button" className="zip-button secondary" onClick={()=>setWizardState(s=>({...s,signers:[...s.signers,{name:'',email:'',order:s.signers.length+1,platform:'autentique'}]}))}><Plus size={14}/> Adicionar signatário</button></Section>}
      {step===6&&<><Section title="Revisão"><div className="reference-form-grid"><label className="wide"><span>Título do Contrato *</span><input value={wizardState.title} onChange={e=>setWizardState(s=>({...s,title:e.target.value}))} placeholder="Ex: Contrato de Empresariamento Artístico"/></label><label><span>Data de Início *</span><input type="date" value={wizardState.start} onChange={e=>setWizardState(s=>({...s,start:e.target.value}))}/></label><label><span>Data de Término</span><input type="date" value={wizardState.end} onChange={e=>setWizardState(s=>({...s,end:e.target.value}))}/></label><label><span>Status</span><select value={wizardState.status} onChange={e=>setWizardState(s=>({...s,status:e.target.value}))}><option>Rascunho</option><option>Pendente</option><option>Aguardando Assinatura</option><option>Assinado</option><option>Vigente</option></select></label><label className="wide"><span>Observações</span><textarea rows={4} value={wizardState.notes} onChange={e=>setWizardState(s=>({...s,notes:e.target.value}))} placeholder="Observações internas sobre este contrato…"/></label></div></Section><div className="reference-contract-preview"><div className="reference-a4"><h1>{wizardState.title||wizardState.template}</h1><p><strong>CONTRATANTE:</strong> {wizardState.contractor}</p><p><strong>CONTRATADA:</strong> {wizardState.contracted||'—'}</p><h2>OBJETO</h2><p>{wizardState.content||'Conteúdo do contrato conforme template selecionado.'}</p></div></div></>}
    </div></div><footer className="reference-modal-footer"><button className="zip-button secondary" onClick={()=>step>1?setStep(s=>s-1):setWizard(false)}>{step>1?'Voltar':'Cancelar'}</button>{step<6?<button className="zip-button" onClick={()=>setStep(s=>Math.min(6,s+1))}>Continuar</button>:<><button className="zip-button secondary" onClick={createContract}>Salvar Rascunho</button><button className="zip-button" onClick={()=>{createContract();setSigning(true)}}><Send size={14}/> Enviar para Assinatura</button></>}</footer></section></div>}

    {viewing&&<div className="reference-modal-backdrop"><section className="reference-modal reference-modal-xl"><header className="reference-modal-head"><div><span>CONTRATO</span><h2>{viewing.title}</h2></div><button className="reference-modal-close" onClick={()=>setViewing(null)}><X size={17}/></button></header><div className="reference-modal-body reference-view-stack"><div className="zip-tabs">{[['resumo','Resumo'],['assinatura','Assinatura'],['documento','Documento'],['historico','Histórico']].map(([key,label])=><button key={key} className={viewTab===key?'active':''} onClick={()=>setViewTab(key)}>{label}</button>)}</div>{viewTab==='resumo'&&<><Section title="Informações"><div className="reference-detail-grid"><div><span>Tipo</span><strong>{viewing.type}</strong></div><div><span>Status</span><strong>{viewing.status}</strong></div><div><span>Plataforma</span><strong>{viewing.platform}</strong></div><div><span>Período</span><strong>{new Date(viewing.start).toLocaleDateString('pt-BR')} – {new Date(viewing.end).toLocaleDateString('pt-BR')}</strong></div><div><span>Valor</span><strong>{money(viewing.value)}</strong></div><div><span>Cliente</span><strong>{viewing.client}</strong></div></div></Section><Section title="Observações"><p>Contrato operacional vinculado ao módulo de contratos e documentação legal.</p></Section></>}{viewTab==='assinatura'&&<><Section title="Assinatura Digital"><div className="reference-detail-grid"><div><span>Provedor</span><strong>{viewing.platform}</strong></div><div><span>Status</span><strong>{viewing.status}</strong></div></div><button className="zip-button" onClick={()=>setSigning(true)}><Send size={14}/> Enviar para Assinatura</button></Section><Section title="Processo de Assinatura Digital"><div className="reference-timeline"><div><span/><div><strong>Documento preparado</strong><small>Hoje · Administrador local</small></div></div>{viewing.status==='Aguardando Assinatura'&&<div><span/><div><strong>Enviado para assinatura</strong><small>Provedor: {viewing.platform}</small></div></div>}{viewing.status==='Assinado'&&<div><span/><div><strong>Assinado</strong><small>Assinatura concluída</small></div></div>}</div></Section></>}{viewTab==='documento'&&<div className="reference-contract-preview"><div className="reference-a4"><h1>{viewing.title}</h1><p><strong>CONTRATANTE:</strong> Portal Lander.</p><p><strong>CONTRATADA:</strong> {viewing.client}.</p><h2>CLÁUSULA PRIMEIRA</h2><p>Objeto e condições conforme registro contratual.</p><h2>VALOR</h2><p>{money(viewing.value)}</p></div></div>}{viewTab==='historico'&&<div className="reference-timeline"><div><span/><div><strong>Contrato criado</strong><small>Registro inicial</small></div></div><div><span/><div><strong>Versão revisada</strong><small>Alteração de conteúdo</small></div></div><div><span/><div><strong>Status atualizado</strong><small>{viewing.status}</small></div></div></div>}</div><footer className="reference-modal-footer"><button className="zip-button secondary" onClick={()=>setViewing(null)}>Fechar</button><button className="zip-button" onClick={()=>setSigning(true)}><Send size={14}/> Enviar para Assinatura</button></footer></section></div>}

    {signing&&<div className="reference-modal-backdrop"><section className="reference-modal"><header className="reference-modal-head"><div><span>ASSINATURA DIGITAL</span><h2>Enviar para Assinatura Digital</h2></div><button className="reference-modal-close" onClick={()=>setSigning(false)}><X size={17}/></button></header><div className="reference-modal-body reference-form"><div className="reference-form-grid"><label className="wide"><span>Provedor de Assinatura</span><select><option>Autentique</option><option>Clicksign</option><option>DocuSign</option></select></label><label className="wide"><span>Mensagem aos signatários</span><textarea rows={4} value={signMessage} onChange={e=>setSignMessage(e.target.value)}/></label><label><span>Prazo para assinatura</span><input type="date" value={signDeadline} onChange={e=>setSignDeadline(e.target.value)}/></label><label><span>Lembretes automáticos</span><select><option>Ativado</option><option>Desativado</option></select></label></div></div><footer className="reference-modal-footer"><button className="zip-button secondary" onClick={()=>setSigning(false)}>Cancelar</button><button className="zip-button" onClick={sendForSigning}><Send size={14}/> Enviar para Assinatura</button></footer></section></div>}
  </AdminShell>
}
