import {Archive,ArrowRightLeft,CalendarDays,CheckCircle2,Clock,Globe2,Headphones,Link,MessageCircle,Mic,Paperclip,Search,Send,Tag,UserPlus} from 'lucide-react'
import {useMemo,useRef,useState} from 'react'
import {LeadFormModal} from '../../crm/LeadFormModal'
import {ContactFormModal} from '../../crm/ContactFormModal'
import {crmRepository} from '../../crm/repository'
import AgendaFormModal from '../../agenda/components/AgendaFormModal'
import {agendaRepository} from '../../agenda/repository'
import {DEADLINE_LABEL,SUPPORT_CHANNEL_OPTIONS,SUPPORT_STATUS_OPTIONS,TEAM_MEMBERS,channelLabel,statusLabel} from '../constants'
import type {ChatAttachment,SupportConversation,SupportMessage,SupportStatus} from '../domain'
import {chatRepository} from '../repository'

type Props={conversations:SupportConversation[];messages:SupportMessage[];quickReplies:string[];selectedId:string;onSelect:(id:string)=>void;onChange:()=>void}
const initials=(value:string)=>value.split(/\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase()
const messagingChannels=[{id:'facebook',label:'Facebook',icon:MessageCircle},{id:'instagram',label:'Instagram',icon:MessageCircle},{id:'tiktok',label:'TikTok',icon:Globe2},{id:'site',label:'Website',icon:Globe2}]
const isClosed=(status:SupportStatus)=>status==='resolvida'||status==='arquivada'

export default function SupportCenterView({conversations,messages,quickReplies,selectedId,onSelect,onChange}:Props){
 const [search,setSearch]=useState(''),[channel,setChannel]=useState('todos'),[status,setStatus]=useState('todos'),[draft,setDraft]=useState(''),[attachments,setAttachments]=useState<ChatAttachment[]>([]),[recording,setRecording]=useState(false),[recordSeconds,setRecordSeconds]=useState(0),[transferOpen,setTransferOpen]=useState(false),[transferTarget,setTransferTarget]=useState(''),[tagOpen,setTagOpen]=useState(false),[tagDraft,setTagDraft]=useState(''),[leadOpen,setLeadOpen]=useState(false),[contactOpen,setContactOpen]=useState(false),[eventOpen,setEventOpen]=useState(false)
 const timerRef=useRef<number|undefined>(undefined)
 const selected=conversations.find(c=>c.id===selectedId)??conversations[0]??null
 const filtered=useMemo(()=>conversations.filter(c=>{const q=search.trim().toLowerCase();return(!q||`${c.customer} ${c.protocol} ${c.tags.join(' ')}`.toLowerCase().includes(q))&&(channel==='todos'||c.channel===channel)&&(status==='todos'||c.status===status)}),[conversations,search,channel,status])
 const selectedMessages=selected?messages.filter(m=>m.conversationId===selected.id):[]
 const stats={new:conversations.filter(c=>c.status==='nova').length,resolved:conversations.filter(c=>c.status==='resolvida').length,unanswered:conversations.filter(c=>c.unread>0).length}
 const refresh=()=>onChange()
 const send=(internal=false)=>{if(!selected)return;const body=draft.trim()||(`${attachments.length} anexo(s) enviado(s).`);if(!body&&!attachments.length)return;chatRepository.sendSupport(selected.id,body,attachments,internal);setDraft('');setAttachments([]);refresh()}
 const startStopRecording=()=>{if(recording){window.clearInterval(timerRef.current);setRecording(false);setAttachments(a=>[...a,{id:`audio_${Date.now()}`,name:`audio-${recordSeconds||1}s.webm`,type:'audio/webm',size:64000}]);setRecordSeconds(0);return}setRecording(true);timerRef.current=window.setInterval(()=>setRecordSeconds(v=>v+1),1000)}
 const fileChange=(files:FileList|null)=>{if(!files)return;setAttachments(a=>[...a,...Array.from(files).map(file=>({id:`att_${Date.now()}_${file.name}`,name:file.name,type:file.type||'application/octet-stream',size:file.size}))])}
 const setConversationStatus=(next:SupportStatus)=>{if(!selected)return;chatRepository.setStatus(selected.id,next);refresh()}
 const transfer=()=>{if(!selected||!transferTarget)return;chatRepository.transfer(selected.id,transferTarget);setTransferOpen(false);setTransferTarget('');refresh()}
 const addTag=()=>{if(!selected||!tagDraft.trim())return;chatRepository.addTag(selected.id,tagDraft);setTagDraft('');setTagOpen(false);refresh()}
 if(!selected)return <div className="chat-card chat-support-empty"><Headphones size={44}/><strong>Nenhuma conversa encontrada</strong></div>
 const agendaParticipants=agendaRepository.participants(),agendaLocations=agendaRepository.locations()
 return <>
  <div className="chat-support-layout">
   <section className="chat-card chat-inbox"><header><div><Headphones size={16}/><div><strong>Atendimento</strong><span>Filas, canais e conversas multicanal.</span></div></div><em>{filtered.length} abertas</em></header><div className="chat-channels"><b>CANAIS DE MENSAGENS</b><div>{messagingChannels.map(({id,label,icon:Icon})=><article key={id}><span><Icon size={12}/>{label}</span><small>mensagens indisponíveis</small></article>)}</div></div><label className="chat-search-field"><Search size={15}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar cliente, protocolo ou tag..."/></label><div className="chat-filter-grid"><select value={channel} onChange={e=>setChannel(e.target.value)}><option value="todos">Todos os canais</option>{SUPPORT_CHANNEL_OPTIONS.map(o=><option key={o.value} value={o.value}>{o.label}</option>)}</select><select value={status} onChange={e=>setStatus(e.target.value)}><option value="todos">Todos status</option>{SUPPORT_STATUS_OPTIONS.map(o=><option key={o.value} value={o.value}>{o.label}</option>)}</select></div><div className="chat-mini-stats"><span><b>{stats.new}</b>Novas</span><span><b>{stats.resolved}</b>Resolvidas</span><span><b>{stats.unanswered}</b>Sem resp.</span></div><div className="chat-support-list">{filtered.length===0?<p>Nenhuma conversa corresponde aos filtros atuais.</p>:filtered.map(c=><button key={c.id} className={c.id===selected.id?'active':''} onClick={()=>onSelect(c.id)}><i>{initials(c.customer)}</i><div><strong>{c.customer}</strong><small>{c.handle}</small><span>{c.lastMessage}</span><em>{channelLabel(c.channel)}</em></div><time>{c.lastMessageAt}</time>{c.unread>0&&<b>{c.unread}</b>}</button>)}</div></section>
   <section className="chat-card chat-conversation"><header className="chat-conversation-head"><div><i>{initials(selected.customer)}</i><div><strong>{selected.customer}</strong><span><em>{channelLabel(selected.channel)}</em><em className={`status-${selected.status}`}>{statusLabel(selected.status)}</em><small>{selected.protocol}</small></span></div></div><div className="chat-conversation-actions"><div className="chat-popover-wrap"><button className="button outline" disabled={isClosed(selected.status)} onClick={()=>setTransferOpen(v=>!v)}><ArrowRightLeft size={13}/>Transferir</button>{transferOpen&&<div className="chat-popover"><label>Transferir para<select value={transferTarget} onChange={e=>setTransferTarget(e.target.value)}><option value="">Selecionar responsável</option>{TEAM_MEMBERS.map(m=><option key={m}>{m}</option>)}</select></label><button className="button dark" disabled={!transferTarget} onClick={transfer}>Confirmar transferência</button></div>}</div><button className="button dark" disabled={isClosed(selected.status)} onClick={()=>setConversationStatus('resolvida')}><CheckCircle2 size={13}/>Finalizar</button></div></header>
    <div className="chat-message-stream support">{selectedMessages.map(m=><div key={m.id} className={`chat-support-message ${m.sender}${m.internalNote?' note':''}`}><div><span>{m.internalNote?'Nota interna':m.author}</span><time>{m.time}</time></div><p>{m.body}</p>{m.deliveryStatus==='failed'&&<small className="failed">Falha no envio{m.deliveryError?`: ${m.deliveryError}`:''}</small>}{m.deliveryStatus==='internal_only'&&!m.internalNote&&<small>Registrado apenas internamente — canal sem envio externo real</small>}{m.attachments?.length&&<section className="chat-message-attachments">{m.attachments.map(a=><article key={a.id}><Paperclip size={12}/><span>{a.name}</span><small>{Math.ceil(a.size/1024)} KB</small></article>)}</section>}</div>)}</div>
    {isClosed(selected.status)?<div className="chat-closed-composer"><strong>Conversa finalizada</strong><span>Reabra a conversa para enviar novas mensagens.</span><button className="button dark" onClick={()=>setConversationStatus('em_atendimento')}>Reabrir</button></div>:<div className="chat-composer">{attachments.length>0&&<div className="chat-pending-files">{attachments.map(a=><span key={a.id}>{a.name}<button onClick={()=>setAttachments(x=>x.filter(y=>y.id!==a.id))}>×</button></span>)}</div>}{recording&&<div className="chat-recording"><Mic size={14}/><div><strong>Gravação de áudio em andamento</strong><span>{String(Math.floor(recordSeconds/60)).padStart(2,'0')}:{String(recordSeconds%60).padStart(2,'0')} · O áudio será anexado à mensagem.</span></div><button className="button outline" onClick={startStopRecording}>Parar e anexar</button></div>}<textarea value={draft} onChange={e=>setDraft(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}}} placeholder="Digite uma mensagem para o cliente..." rows={3}/><div className="chat-composer-tools"><label className="chat-tool-button" aria-label="Anexar arquivo"><Paperclip size={14}/><input type="file" multiple onChange={e=>fileChange(e.target.files)}/></label><button className="chat-tool-button" onClick={startStopRecording} aria-label="Gravar áudio"><Mic size={14}/></button><select aria-label="Respostas rápidas" value="" onChange={e=>setDraft(e.target.value)}><option value="">Resposta rápida</option>{quickReplies.map(q=><option key={q} value={q}>{q}</option>)}</select><button className="button outline" disabled={!draft.trim()} onClick={()=>send(true)}>Nota interna</button><button className="button dark" disabled={!draft.trim()&&!attachments.length} onClick={()=>send()}><Send size={14}/>Enviar</button></div></div>}
   </section>
   <aside className="chat-card chat-ops"><header><strong>Painel Operacional</strong><span>Cliente, prazo, CRM e acoes da conversa.</span></header><section><h3>Cliente</h3><div className="chat-client"><i>{initials(selected.customer)}</i><div><strong>{selected.customer}</strong><span>{selected.handle}</span></div></div><dl><div><dt>Telefone</dt><dd>{selected.phone||'—'}</dd></div><div><dt>Instagram</dt><dd>{selected.instagram||'—'}</dd></div><div><dt>E-mail</dt><dd>{selected.email||'—'}</dd></div><div><dt>Canal de origem</dt><dd>{selected.originLabel}</dd></div>{selected.assunto&&<div><dt>Assunto</dt><dd>{selected.assunto}</dd></div>}</dl></section><section><h3>Conversa</h3><dl><div><dt>Protocolo</dt><dd>{selected.protocol}</dd></div><div><dt>Status</dt><dd>{statusLabel(selected.status)}</dd></div><div><dt>Fila</dt><dd>{selected.queue||'—'}</dd></div><div><dt>Setor</dt><dd>{selected.sector||'—'}</dd></div><div><dt>Responsável</dt><dd>{selected.assignee}</dd></div></dl></section><section><h3>Prazo de Atendimento</h3><div className={`chat-sla ${selected.deadlineState}`}><strong>{DEADLINE_LABEL[selected.deadlineState]}</strong><div><span style={{width:`${Math.max(0,Math.min(100,selected.sla))}%`}}/></div><small>{selected.sla}% restante · {selected.remainingTimeLabel}</small></div></section><section><h3>Tags</h3><div className="chat-tags">{selected.tags.map(tag=><span key={tag}><Tag size={10}/>{tag}</span>)}<div className="chat-popover-wrap"><button onClick={()=>setTagOpen(v=>!v)}>+ Adicionar Tag</button>{tagOpen&&<div className="chat-popover tag"><label>Nova tag<input value={tagDraft} onChange={e=>setTagDraft(e.target.value)} placeholder="Ex.: Prioridade"/></label><button className="button dark" disabled={!tagDraft.trim()} onClick={addTag}>Adicionar</button></div>}</div></div></section><section><h3>Ações</h3><div className="chat-action-grid"><button onClick={()=>setLeadOpen(true)}><UserPlus size={13}/>Criar Lead</button><button onClick={()=>setContactOpen(true)}><Link size={13}/>Vincular CRM</button><button onClick={()=>setEventOpen(true)}><CalendarDays size={13}/>Criar Evento</button><button disabled={isClosed(selected.status)} onClick={()=>setTransferOpen(true)}><ArrowRightLeft size={13}/>Transferir</button><button disabled={selected.status==='arquivada'} onClick={()=>setConversationStatus('arquivada')}><Archive size={13}/>Arquivar</button><button disabled={isClosed(selected.status)} onClick={()=>setConversationStatus('resolvida')}><CheckCircle2 size={13}/>Finalizar</button><button onClick={()=>{chatRepository.escalate(selected.id);refresh()}}><Clock size={13}/>Escalonar</button></div></section><section className="chat-crm-summary"><h3>CRM</h3><dl><div><dt>Cliente existente</dt><dd>{selected.crmSummary.existingCustomer?'SIM':'NAO'}</dd></div><div><dt>Lead</dt><dd>{selected.crmSummary.lead||'—'}</dd></div><div><dt>Negócio aberto</dt><dd>{selected.crmSummary.openDeal||'—'}</dd></div><div><dt>Etapa</dt><dd>{selected.crmSummary.stage||'—'}</dd></div></dl></section><section><h3>Histórico</h3><dl><div><dt>Criado</dt><dd>{selected.createdAt}</dd></div><div><dt>Última resposta</dt><dd>{selected.lastReplyAt}</dd></div><div><dt>Canal</dt><dd>{selected.originLabel}</dd></div><div><dt>Status</dt><dd>{statusLabel(selected.status)}</dd></div><div><dt>Fila</dt><dd>{selected.queue}</dd></div><div><dt>Responsável</dt><dd>{selected.assignee}</dd></div></dl></section></aside>
  </div>
  <LeadFormModal open={leadOpen} onClose={()=>setLeadOpen(false)} busy={false} onSubmit={async draft=>{const lead=crmRepository.createLead(draft as Parameters<typeof crmRepository.createLead>[0]);chatRepository.markCrm(selected.id,{lead:lead.name,existingCustomer:true});setLeadOpen(false);refresh()}}/>
  <ContactFormModal open={contactOpen} onClose={()=>setContactOpen(false)} busy={false} onSubmit={async draft=>{crmRepository.createContact(draft as Parameters<typeof crmRepository.createContact>[0]);chatRepository.markCrm(selected.id,{existingCustomer:true});setContactOpen(false);refresh()}}/>
  <AgendaFormModal open={eventOpen} mode="create" participants={agendaParticipants} locations={agendaLocations} onClose={()=>setEventOpen(false)} onSave={draft=>{agendaRepository.create(draft);setEventOpen(false)}}/>
 </>
}
