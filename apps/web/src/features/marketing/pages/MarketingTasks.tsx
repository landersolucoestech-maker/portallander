import {AlertTriangle,CalendarClock,CheckCircle2,ListChecks,MoreHorizontal,Plus} from 'lucide-react'
import {useState,type FormEvent} from 'react'
import {dateLabel,type MarketingSeed,type MarketingTask,type Priority,type TaskStatus} from '../domain'
import {marketingRepository} from '../repository'
import {ActionMenu,Empty,Field,Filters,Kpi,Modal,ModalFooter,Pagination,SearchBox,SelectField,Status,ViewModal,paginate,statusLabel} from '../MarketingUi'
export function MarketingTasks({state}:{state:MarketingSeed}){const [query,setQuery]=useState(''),[type,setType]=useState('all'),[page,setPage]=useState(1),[selected,setSelected]=useState<string[]>([]),[editor,setEditor]=useState<MarketingTask|null|undefined>(undefined),[view,setView]=useState<MarketingTask|null>(null);const rows=state.tasks.filter(x=>(!query||`${x.title} ${x.type} ${x.owner} ${x.context}`.toLowerCase().includes(query.toLowerCase()))&&(type==='all'||x.type===type)),paged=paginate(rows,page),allPage=paged.length>0&&paged.every(x=>selected.includes(x.id));const toggleAll=()=>setSelected(v=>allPage?v.filter(id=>!paged.some(x=>x.id===id)):Array.from(new Set([...v,...paged.map(x=>x.id)])));const removeSelected=()=>{selected.forEach(id=>marketingRepository.deleteTask(id));setSelected([])};return <>
<div className="marketing-page-action-row"><button className="marketing-primary marketing-action-button" type="button" onClick={()=>setEditor(null)}><Plus size={14}/>Nova Tarefa</button></div>
<div className="marketing-kpis marketing-kpis-5"><Kpi label="Total de Tarefas" value={state.tasks.length} detail="demandas cadastradas" icon={<ListChecks/>}/><Kpi label="Concluídas" value={state.tasks.filter(x=>x.status==='concluida').length} detail="finalizadas" icon={<CheckCircle2/>}/><Kpi label="A Fazer" value={state.tasks.filter(x=>x.status==='a_fazer'||x.status==='backlog').length} detail="backlog e a fazer" icon={<CalendarClock/>}/><Kpi label="Em Andamento" value={state.tasks.filter(x=>x.status==='em_andamento').length} detail="em execução" icon={<MoreHorizontal/>}/><Kpi label="Revisão" value={state.tasks.filter(x=>x.status==='revisao').length} detail="em revisão" icon={<AlertTriangle/>}/></div>
<Filters><SearchBox value={query} onChange={v=>{setQuery(v);setPage(1)}} placeholder="Buscar tarefa..."/><select value={type} onChange={e=>{setType(e.target.value);setPage(1)}}><option value="all">Todos os tipos</option>{state.taskTypes.map(x=><option key={x}>{x}</option>)}</select></Filters>
<section className="marketing-table-card marketing-table-section"><header className="marketing-list-header"><div><h3>Lista de Tarefas</h3><p>Acompanhe tarefas, tipos, responsáveis, prazos e status · {rows.length} registro(s)</p></div><div className="marketing-selection-tools"><label><input type="checkbox" checked={allPage} onChange={toggleAll}/><span>{selected.length?`${selected.length} tarefa(s) selecionada(s)`:'Selecionar todos'}</span></label>{selected.length>0&&<button type="button" className="marketing-danger-button" onClick={removeSelected}>Remover ({selected.length})</button>}</div></header><div className="marketing-table-wrap"><table className="marketing-table marketing-reference-table marketing-task-table"><thead><tr><th className="select"><span className="sr-only">Selecionar</span></th><th>Tarefa</th><th>Tipo</th><th>Prioridade</th><th>Setor</th><th>Responsável</th><th>Prazo</th><th>Status</th><th className="actions">Ações</th></tr></thead><tbody>{paged.map(x=><tr key={x.id} onClick={()=>setView(x)}><td className="select" onClick={e=>e.stopPropagation()}><input type="checkbox" checked={selected.includes(x.id)} onChange={()=>setSelected(v=>v.includes(x.id)?v.filter(id=>id!==x.id):[...v,x.id])}/></td><td><strong>{x.title}</strong><small>{x.context}</small></td><td>{x.type}</td><td><Status value={x.priority}/></td><td>{x.department}</td><td>{x.owner}</td><td>{dateLabel(x.deadline)}</td><td><Status value={x.status}/></td><td className="actions" onClick={e=>e.stopPropagation()}><ActionMenu onView={()=>setView(x)} onEdit={()=>setEditor(x)} onDelete={()=>marketingRepository.deleteTask(x.id)}/></td></tr>)}</tbody></table><Empty empty={!rows.length} text="Nenhuma tarefa encontrada."/><Pagination page={page} total={rows.length} onChange={setPage}/></div></section>
{editor!==undefined&&<TaskModal state={state} value={editor} onClose={()=>setEditor(undefined)}/>} {view&&<ViewModal title={view.title} onClose={()=>setView(null)} rows={[['Tipo',view.type],['Responsável',view.owner],['Setor',view.department],['Prazo',dateLabel(view.deadline)],['Status',statusLabel(view.status)],['Prioridade',statusLabel(view.priority)],['Contexto',view.context],['Classificação',view.classification],['Observações',view.notes||'—']]}/>}</>}
function TaskModal({state,value,onClose}:{state:MarketingSeed;value:MarketingTask|null;onClose:()=>void}){const [form,setForm]=useState(value?{...value}:{title:'',type:state.taskTypes[0]??'Design',owner:state.owners[0]??'',department:state.departments[0]??'Marketing',deadline:'',status:'a_fazer' as TaskStatus,priority:'media' as Priority,context:'',classification:'',notes:''}),[error,setError]=useState('');const submit=(e:FormEvent)=>{e.preventDefault();if(!form.title.trim()||!form.deadline){setError('Preencha o título e o prazo da tarefa.');return}const {id:_id,createdAt:_createdAt,updatedAt:_updatedAt,...input}=form as MarketingTask;marketingRepository.saveTask(input,value?.id);onClose()};return <Modal title={value?'Editar Tarefa':'Nova Tarefa'} subtitle={value?'Atualize a demanda operacional e seus responsáveis.':'Crie uma nova demanda para organizar a operação de marketing.'} onClose={onClose}><form onSubmit={submit}><div className="marketing-modal-body"><div className="marketing-form-grid"><Field label="Tarefa *" value={form.title} onChange={v=>setForm({...form,title:v})} wide/><SelectField label="Tipo" value={form.type} onChange={v=>setForm({...form,type:v})} options={state.taskTypes}/><SelectField label="Responsável" value={form.owner} onChange={v=>setForm({...form,owner:v})} options={state.owners}/><SelectField label="Setor" value={form.department} onChange={v=>setForm({...form,department:v})} options={state.departments}/><Field label="Prazo *" type="date" value={form.deadline} onChange={v=>setForm({...form,deadline:v})}/><SelectField label="Status" value={form.status} onChange={v=>setForm({...form,status:v as TaskStatus})} options={['backlog','a_fazer','em_andamento','revisao','concluida','bloqueada']}/><SelectField label="Prioridade" value={form.priority} onChange={v=>setForm({...form,priority:v as Priority})} options={['baixa','media','alta','urgente']}/><Field label="Contexto" value={form.context} onChange={v=>setForm({...form,context:v})}/><Field label="Classificação" value={form.classification} onChange={v=>setForm({...form,classification:v})}/><Field label="Observações" value={form.notes} onChange={v=>setForm({...form,notes:v})} wide/></div>{error&&<p className="marketing-form-error">{error}</p>}{value&&<section className="marketing-deliverables-placeholder"><strong>Entregáveis da tarefa</strong><p>Área preparada para acompanhar arquivos, entregáveis e validações relacionadas a esta demanda.</p></section>}</div><ModalFooter onClose={onClose} label={value?'Salvar alterações':'Criar tarefa'}/></form></Modal>}
