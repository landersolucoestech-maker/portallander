import {X} from 'lucide-react'
import {signingProviders} from '../signing'
import type {Contract,SigningProvider} from '../domain'
export function SendForSigningDialog({contract,onClose}:{contract:Contract;onClose:()=>void}){
 return <div className="crm-modal-backdrop" onMouseDown={e=>{if(e.currentTarget===e.target)onClose()}}><section className="crm-modal contracts-signing-dialog" role="dialog" aria-modal="true" aria-label="Enviar para assinatura"><header className="crm-modal-head"><div><h2>Enviar para assinatura</h2><p>Selecione o provider, revise os signatários e confirme a disponibilidade da integração.</p></div><button className="crm-icon-btn" onClick={onClose} aria-label="Fechar"><X size={18}/></button></header><div className="crm-modal-body contracts-signing-body"><section><h3>Documento</h3><strong>{contract.title}</strong><span>{contract.signers.length} signatário(s) configurado(s)</span></section><section><h3>Providers</h3>{(['autentique','clicksign','docusign'] as SigningProvider[]).map(provider=><button key={provider} className="contracts-provider" disabled={!signingProviders[provider].configured}><strong>{provider==='autentique'?'Autentique':provider==='clicksign'?'Clicksign':'DocuSign'}</strong><span>{signingProviders[provider].configured?'Configurado':'Integração não configurada'}</span></button>)}</section><section><h3>Ordem de assinatura</h3>{contract.signers.length===0?<div className="crm-empty-mini">Nenhum signatário configurado.</div>:contract.signers.sort((a,b)=>a.order-b.order).map(s=><div className="contracts-signer-line" key={s.id}><span>{s.order}</span><strong>{s.name}</strong><small>{s.email}</small></div>)}</section><div className="crm-error">Nenhum provider está configurado neste frontend. Credenciais e envio real devem permanecer no servidor.</div></div><footer className="crm-modal-foot"><button className="crm-btn secondary" onClick={onClose}>Fechar</button></footer></section></div>
}
