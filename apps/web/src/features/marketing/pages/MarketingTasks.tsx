import {CalendarDays,CheckCircle2,FileEdit,ListChecks,MoreHorizontal} from 'lucide-react'
import {useState,type FormEvent} from 'react'
import {dateLabel,type MarketingSeed,type MarketingTask,type Priority,type TaskStatus} from '../domain'
import {marketingRepository} from '../repository'
import {ActionMenu,Empty,Field,Filters,Kpi,Modal,ModalFooter,Pagination,SearchBox,SelectField,Status,TableCard,ViewModal,paginate,statusLabel} from '../MarketingUi'
export function MarketingTasks({state}:{state:MarketingSeed}){const [query,setQuery]=useState(''),[status,setStatus]=useState('all'),[type,setType]=useState('all'),[page,setPage]=useState(1),[editor,setEditor]=useState<MarketingTask|null|undefined>(undefined),[view,setView]=useState<MarketingTask|null>(null);const rows=state.tasks.filter(x=>(!query||`${x.title} ${x.type} ${x.owner} ${x.context}`.toLowerCase().includes(query.toLowerCase()))&&(status==='all'||x.status===status)&&(type==='all'||x.type===type)),paged=paginate(rows,page);return <><div className="marketing-kpis marketing-kpis-5"><Kpi label="Total de Tarefas" value={state.tasks.length} detail="demandas cadastradas" icon={<ListChecks/>}/><Kpi label="Concluídas" value={state.tasks.filter(x=>x.status==='concluida').length} detail="finalizadas" icon={<CheckCircle2/>}/><Kpi label="A Fazer" value={state.tasks.filter(x=>x.status==='a_fazer'||x.status==='backlog').length} detail="backlog e a fazer" icon={<CalendarDays/>}/><Kpi label="Em Andamento" value={state.tasks.filter(x=>x.status==='em_andamento').length} detail="em execução" icon={<MoreHorizontal/>}/><Kpi label="Revisão" value={state.tasks.filter(x=>x.status==='revisao').length} detail="em revisão" icon={<FileEdit/>}/></div><Filters><SearchBox value={query} onChange={v=>{setQuery(v);setPage(1)}} placeholder="Buscar tarefa..."/><select value={type} onChange={e=>setType(e.target.value)}><option value="all">Todos os tipos</option>{state.taskTypes.map(x=><option key={x}>{x}</option>)}</select><select value={status} onChange={e=>setStatus(e.target.value)}><option value="all">Todos os status</option>{['backlog','a_fazer','em_andamento','revisao','concluida','bloqueada'].map(x=><option key={x} value={x}>{statusLabel(x)}</option>)}</select></Filters><TableCard title="Lista de Tarefas" description="Acompanhe tarefas, tipos, responsáveis, prazos e status" count={rows.length}><table className="marketing-table"><thead><tr><th>Tarefa</th><th>Tipo</th><th>Prioridade</th><th>Setor</th><th>Responsável</th><th>Prazo</th><th>Status</th><th className="actions">Ações</th></tr></thead><tbody>{paged.map(x=><tr key={x.id}><td><strong>{x.title}</strong><small>{x.context}</small></td><td>{x.type}</td><td><Status value={x.priority}/></td><td>{x.department}</td><td>{x.owner}</td><td>{dateLabel(x.deadline)}</td><td><Status value={x.status}/></td><td className="actions"><ActionMenu onView={()=>setView(x)} onEdit={()=>setEditor(x)} onDelete={()=>marketingRepository.deleteTask(x.id)}/></td></tr>)}</tbody></table><Empty empty={!rows.length} text="Nenhuma tarefa encontrada."/><Pagination page={page} total={rows.length} onChange={setPage}/></TableCard><button className="marketing-floating-create" type="button" onClick={()=>setEditor(null)}>+ Nova Tarefa</button>{editor!==undefined&&<TaskModal state={state} value={editor} onClose={()=>setEditor(undefined)}/>} {view&&<ViewModal title={view.title} onClose={()=>setView(null)} rows={[['Tipo',view.type],['Responsável',view.owner],['Setor',view.department],['Prazo',dateLabel(view.deadline)],['Status',statusLabel(view.status)],['Prioridade',statusLabel(view.priority)],['Contexto',view.context],['Classificação',view.classification],['Observações',view.notes||'—']]}/>}</>}
function TaskModal({state,value,onClose}:{state:MarketingSeed;value:MarketingTask|null;onClose:()=>void}){const [form,setForm]=useState(value?{...value}:{title:'',type:state.taskTypes[0]??'Design',owner:state.owners[0]??'',department:state.departments[0]??'Marketing',deadline:'',status:'a_fazer' as TaskStatus,priority:'media' as Priority,context:'',classification:'',notes:''}),[error,setError]=useState('');const submit=(e:FormEvent)=>{e.preventDefault();if(!form.title.trim()||!form.deadline){setError('Preencha o título e o prazo da tarefa.');return}const {id:_id,createdAt:_createdAt,updatedAt:_updatedAt,...input}=form as MarketingTask;marketingRepository.saveTask(input,value?.id);onClose()};return <Modal title={value?'Editar Tarefa':'Nova Tarefa'} subtitle="Organize a demanda operacional do marketing" onClose={onClose}><form onSubmit={submit}><div className="marketing-modal-body"><div className="marketing-form-grid"><Field label="Tarefa *" value={form.title} onChange={v=>setForm({...form,title:v})}/><SelectField label="Tipo" value={form.type} onChange={v=>setForm({...form,type:v})} options={state.taskTypes}/><SelectField label="Responsável" value={form.owner} onChange={v=>setForm({...form,owner:v})} options={state.owners}/><SelectField label="Setor" value={form.department} onChange={v=>setForm({...form,department:v})} options={state.departments}/><Field label="Prazo *" type="date" value={form.deadline} onChange={v=>setForm({...form,deadline:v})}/><SelectField label="Status" value={form.status} onChange={v=>setForm({...form,status:v as TaskStatus})} options={['backlog','a_fazer','em_andamento','revisao','concluida','bloqueada']}/><SelectField label="Prioridade" value={form.priority} onChange={v=>setForm({...form,priority:v as Priority})} options={['baixa','media','alta','urgente']}/><Field label="Contexto" value={form.context} onChange={v=>setForm({...form,context:v})}/><Field label="Classificação" value={form.classification} onChange={v=>setForm({...form,classification:v})}/><Field label="Observações" value={form.notes} onChange={v=>setForm({...form,notes:v})} wide/></div>{error&&<p className="marketing-form-error">{error}</p>}</div><ModalFooter onClose={onClose}/></form></Modal>}
