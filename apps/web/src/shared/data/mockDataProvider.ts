import {mockAdvertisingCampaigns,mockAdvertisingFormats,mockAgendaItems,mockAgendaLocations,mockAgendaParticipants,mockBrandingConfig,mockChatSeed,mockCollaborationGuidelines,mockCollaborationTypes,mockContractCategories,mockContracts,mockContractTemplates,mockContractVariables,mockCurrentUserId,mockDashboardOperationalSnapshot,mockDataScenarios,mockDefaultHeroConfig,mockDefaultHeroSlide,mockDefaultHomeAdConfig,mockDefaultNewsAdConfig,mockEditorialContents,mockEditorialMedia,mockEditorialPages,mockFinanceCategories,mockFinanceInvoices,mockFinanceRules,mockFinanceTransactions,mockHeroArticles,mockHomeAgenda,mockHomeMostRead,mockHomeReleases,mockHomeStories,mockNotifications,mockSchedulerEvents,mockSocialChannels,mockUsers,mockWorkspaces,mockCrmState,mockRhSeed} from '../../mocks'
import type {DataScenarioName} from './contracts'
import type {ApplicationDataProvider,DataDomain} from './dataProvider'

const SCENARIO_STORAGE_KEY='portal-lander:data-scenario:v1'
const LARGE_DATASET_FACTOR=4
const clone=<T>(value:T):T=>structuredClone(value)
const readScenario=():DataScenarioName=>{try{const value=localStorage.getItem(SCENARIO_STORAGE_KEY) as DataScenarioName|null;return value&&value in mockDataScenarios?value:'success'}catch{return 'success'}}
let scenarioName:DataScenarioName=readScenario()
const scenario=()=>mockDataScenarios[scenarioName]
const guard=(domain:DataDomain)=>{const current=scenario();if(current.permissionDeniedDomains.includes(domain))throw new Error(`Permission denied for data domain: ${domain}`);if(current.failDomains.includes(domain))throw new Error(`Mock data failure for domain: ${domain}`)}
const withScaledId=<T>(item:T,copyIndex:number):T=>{if(copyIndex===0||!item||typeof item!=='object'||!('id' in item))return clone(item);const record=item as Record<string,unknown>;return {...clone(record),id:`${String(record.id)}__large_${copyIndex+1}`} as T}
const scale=<T>(items:readonly T[]):T[]=>Array.from({length:LARGE_DATASET_FACTOR},(_,copyIndex)=>items.map(item=>withScaledId(item,copyIndex))).flat()
const list=<T>(domain:DataDomain,items:readonly T[]):T[]=>{guard(domain);const current=scenario();if(current.emptyDomains.includes(domain))return[];if(current.partialDomains.includes(domain))return clone(items.slice(0,Math.max(1,Math.ceil(items.length/3))) as T[]);if(current.largeDataset)return scale(items);return clone(items as T[])}
const scaleCrmState=()=>({version:1 as const,leads:scale(mockCrmState.leads),contacts:scale(mockCrmState.contacts)})
const scaleContractsState=()=>({contracts:scale(mockContracts),templates:clone(mockContractTemplates),categories:clone(mockContractCategories),variables:clone(mockContractVariables)})
export const mockDataProvider:ApplicationDataProvider={kind:'mock',getScenario:()=>clone(scenario()),setScenario(name){scenarioName=name;try{localStorage.setItem(SCENARIO_STORAGE_KEY,name)}catch{/* storage unavailable */}},identity:{users:()=>list('identity',mockUsers),currentUser:()=>{guard('identity');const user=mockUsers.find(item=>item.id===mockCurrentUserId);if(!user)throw new Error('Mock current user does not exist.');return clone(user)},workspaces:()=>list('identity',mockWorkspaces)},notifications:{list:()=>list('notifications',mockNotifications)},crm:{state:()=>{guard('crm');const current=scenario();if(current.emptyDomains.includes('crm'))return{version:1,leads:[],contacts:[]};if(current.partialDomains.includes('crm'))return{version:1,leads:clone(mockCrmState.leads.slice(0,12)),contacts:clone(mockCrmState.contacts.slice(0,12))};if(current.largeDataset)return scaleCrmState();return clone(mockCrmState)}},contracts:{state:()=>{guard('contracts');const current=scenario();if(current.largeDataset)return scaleContractsState();return{contracts:current.emptyDomains.includes('contracts')?[]:clone(current.partialDomains.includes('contracts')?mockContracts.slice(0,8):mockContracts),templates:clone(mockContractTemplates),categories:clone(mockContractCategories),variables:clone(mockContractVariables)}}},finance:{transactions:()=>list('finance',mockFinanceTransactions),invoices:()=>list('finance',mockFinanceInvoices),categories:()=>list('finance',mockFinanceCategories),rules:()=>list('finance',mockFinanceRules)},editorial:{pages:()=>list('editorial',mockEditorialPages),contents:()=>list('editorial',mockEditorialContents),media:()=>list('editorial',mockEditorialMedia)},home:{stories:()=>list('home',mockHomeStories),mostRead:()=>list('home',mockHomeMostRead),releases:()=>list('home',mockHomeReleases),agenda:()=>list('home',mockHomeAgenda),heroArticles:()=>list('home',mockHeroArticles),defaultHeroSlide:()=>{guard('home');return clone(mockDefaultHeroSlide)},defaultHeroConfig:()=>{guard('home');return clone(mockDefaultHeroConfig)}},agenda:{items:()=>list('agenda',mockAgendaItems),events:()=>list('agenda',mockSchedulerEvents),participants:()=>list('agenda',mockAgendaParticipants),locations:()=>list('agenda',mockAgendaLocations)},chat:{seed:()=>{guard('chat');const current=scenario();if(current.emptyDomains.includes('chat'))return{...clone(mockChatSeed),supportConversations:[],supportMessages:[],internalConversations:[],internalMessages:[]};return clone(mockChatSeed)}},rh:{seed:()=>{guard('rh');const current=scenario();if(current.emptyDomains.includes('rh'))return{...clone(mockRhSeed),employees:[],payroll:[],leaves:[],documents:[]};return clone(mockRhSeed)}},advertising:{campaigns:()=>list('advertising',mockAdvertisingCampaigns),formats:()=>list('advertising',mockAdvertisingFormats),defaultHomeAdConfig:()=>{guard('advertising');return clone(mockDefaultHomeAdConfig)},defaultNewsAdConfig:()=>{guard('advertising');return clone(mockDefaultNewsAdConfig)}},branding:{config:()=>{guard('branding');return clone(mockBrandingConfig)},socialChannels:()=>list('branding',mockSocialChannels)},collaboration:{types:()=>list('collaboration',mockCollaborationTypes),guidelines:()=>list('collaboration',mockCollaborationGuidelines)},dashboard:{operationalSnapshot:()=>{guard('dashboard');return clone(mockDashboardOperationalSnapshot)}}}
